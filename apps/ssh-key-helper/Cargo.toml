[package]
name = "sigmagit-ssh-key-helper"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ureq = { version = "2.10", features = ["json"] }

[profile.release]
opt-level = 3
lto = "thin"
strip = true
